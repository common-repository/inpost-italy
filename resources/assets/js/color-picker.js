jQuery(document).ready(function($){
	$('#easypack_italy_custom_button_css').wpColorPicker({
		defaultColor: false,
		change: function(event, ui){ },
		clear: function(){ },
		hide: true,
		palettes: true
	});
});